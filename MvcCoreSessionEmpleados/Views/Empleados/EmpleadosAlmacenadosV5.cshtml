
@model List<Empleado>

<h1 style="color:orangered">
    Empleados almacenados V5
</h1>
<p>
    <a asp-controller="Empleados"
       asp-action="SessionEmpleadosV5">
        Volver a empleados
    </a>
</p>

@if(Context.Session.GetString("IDSEMPLEADOS") == null)
{
    <h1 style="color:red">
        No existe empleados en Session
    </h1>
}

<h2 style="color:orangered">@ViewData["MENSAJE"]</h2>

@if (Model != null)
{
    <table class="table table-primary">
        <thead>
            <tr>
                <th>Id</th>
                <th>Apellido</th>
                <th>Oficio</th>
                <th>Salario</th>
                <th>Departamento</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.IdEmpleado</td>
                    <td>@item.Apellido</td>
                    <td>@item.Oficio</td>
                    <td>@item.Salario</td>
                    <td>@item.IdDepartamento</td>
                    <td>
                        <a asp-controller="Empleados"
                           asp-action="SessionEmpleadosV5" class="btn btn-dark"
                           asp-route-ideliminar="@item.IdEmpleado">
                            Eliminar empleado Session
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}